// Admin analytics shares the same dashboard as the recruiter analytics view.
// The analyticsService can be swapped per-role when backend scoping is added.
export { default } from "@/app/(app)/recruiter/analytics/page"
